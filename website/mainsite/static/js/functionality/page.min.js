
    function createNewMultipleChoice() {
      alert('Create New Multiple Choice Question');
      closeModal();
    }

    function createNewQuestion() {
      alert('Create New Question');
      closeModal();
    }

    function closeModal() {
      var modal = document.getElementById('contentModal');
      modal.style.display = 'none';
    }

     function openModal() {
        //determine if modal is open
        var modal = document.getElementById('contentModal');
        modal.style.display = 'block';
    }

    //TODO: create functionalit to close quetions modal when not in use

    //open new multiple choice question gateway
    function CNMQ(){
        return window.location.replace('/lms/newmultquestions')
    }

    //open new standard question gateway
    function CNQ(){
//        return redirect('/')
    }